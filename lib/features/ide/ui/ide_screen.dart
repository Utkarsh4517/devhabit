import 'package:flutter/material.dart';

class IdeScreen extends StatelessWidget {
  const IdeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('IdeScreen '),
      ),
    );
  }
}

